# 烛龙 ZHULONG · 设计与交接说明（浅色 · 决策先行一栏多层版）

> 文件：`zhulong.html`（单文件，ECharts CDN，零构建）
> 产出日期：2026-08-28 · 终稿：2026-08-29（含外部 AI 评审 10 项修复）
> 状态：**设计定稿**，待接真数据

## 一句话

不是仪表盘，是**调度员的驾驶舱 + 一台可以拨动时间的机器**：别人展示预测曲线，烛龙让评委亲手拨动时间、亲眼验证预测。

## 信息架构原则（为什么是这些数据）

页面按**用户问题的时间序列**组织，每个模块必须回答其中一个问题，回答不了的元素不占屏位：

| 问题 | 回答模块 |
|---|---|
| 现在正常吗？（秒级） | 状态条「现在/偏差」格 + 偏差副带 |
| 接下来 24h 要准备什么？ | 状态条「今日峰」格 + 右栏 01 决策卡 + 主图预备窗 |
| 这个预测可信吗？ | 状态条「可信」格 + 右栏 03 审计卡 |
| 为什么是这个值？ | 右栏 02 归因卡（温度贡献 = Δ温度 × 灵敏度，可复算） |
| 过去表现如何？ | 时光机回放 + 极端日排行 + 热力图 |
| 哪个区域是主因？ | 底部区域对比 |

**每个数字都带参照系**：当前负荷 vs 昨日同时、峰值 vs 近30日均值、预测 vs 历史极值线、误差 vs 标称覆盖率。孤立的数字没有意义。

## 布局（决策先行 · 一栏多层 · 文档流自然滚动）

```
顶栏（吸顶）：烛龙 identity · 区域切换 · 美东时钟 · LIVE · 演示按钮
⓪ 决策层（结论主导·真主角）：⚡建议动作 → 预备窗一行预告 →「预备 2,700 MW 调峰资源」
   关键数字 46px 琥珀压场（.db-hero），其余退小；左色条 = 风险等级
   依据行（3 项）：日峰 P50→P90 · 距纪录 · 区间校准✓ + [依据⋯弹层] + [查看审计↗]
   依据弹层（渐进披露）：较30日 · 爬坡 MW/h · 建议容量公式；与 ⓘ/☰ 弹层互斥
   设计反思：26px 结论句在 4 个 20px 大数字面前不构成层级——塔尖需要数量级的视觉差。
   已删除不可解释的「置信分」，替换为可验证证据链（区间校准 + 审计链接）。
① 状态层（四格对等醒目）：现在│偏差│今日峰(P50@时刻+最坏P90)│可信(MAPE+P90命中，参照"优<3%")
   被裁子指标（P90距纪录、±1.5%口径）移入格子 title；与决策层重复的数字不二次占屏
② 主线层【时空推演 · 时间机器一体卡】（全宽，默认 3 天视窗，主图高 clamp(290px,37vh,400px)）：
   实际(青)·昨日(灰)·NOW·预测扇形·上帝视角·历史极值线·预备窗
   峰值标注两行钉在图上：日峰 P50 xx · 时刻 ／ 最坏 P90 xx · 建议备 xx MW
   [温度带｜偏差带] · 图例单行 chip（语义入 title）· 频率句（脚注级弱化）
   ↕ 紧贴：时光机胶片 dock（#filmDock）——主图是画面、胶片是进度条，同一台时间机器；
     含 实时/重演 chip · 日期胶囊 ‹› · 极端日 chips（❄2014极涡/♨2012热浪/回到当前）
③ 证据层（两张扁卡横排，高度互不影响）：为什么·归因 │ 可信·审计
底部抽屉（区域/极端日/热力图）
（口径 = 主图 ⓘ 弹层 + 依据弹层，页面无展开卡——任何交互都不会改变布局高度）
```

**首屏预算（1440×900）**：决策 + 状态 + 主图卡（含胶片 dock）= **879px，一屏收齐**；
全页高度 1377→1220。第一屏讲"现在与接下来"，第二屏"为什么可信"，第三屏"过去"——叙事动线与滚动方向同构。

**框架原则**（与用户对齐定稿）：页面第一眼 = 「要做什么」（决策层，倒金字塔）；
状态四格紧随其后且对等醒目、不被弱化；重点由全宽大图 + 峰值标注承担，不靠压暗别人。
参照系不常驻：可信度内容按需翻出（弹层/title），"深度是发现的，不是陈列的"。

## 视觉系统（浅色，全部经 CVD 六项验证）

- 背景 `#F5F7FA` · 卡片 `#FFFFFF` · 墨 `#0F172A/#475569/#7C8DA6`；浅色下**零发光**，层次由线宽（主曲线 3px）+ 色深 + 留白承担
- 实际 `#0891B2` · 预测 `#C2620A`（protan ΔE 20.2）；区域色 AEP `#0891B2` / DAYTON `#7C3AED` / DOM `#DB2777`（ΔE ≥ 15）
- 浅色的场地理由：8/30 游园会+路演为亮环境投影，浅色可读性优于深色
- 频率句保留衬线体（叙事声音），数字一律无衬线正比字（大数）/等宽（表格与轴）

## 交互清单

| 交互 | 操作 |
|---|---|
| 区域切换 | 顶栏或底部小图 |
| 时间范围 | 主图头 24h / 3 天 / 7 天 |
| 叠加开关 | ☰（温度带/昨日/上帝视角/峰值与预备窗） |
| 时光机 | 拖动 NOW 手柄（主图卡底部 dock）；‹ › 步进；点胶片跳转 |
| 极端日 | 快捷 chips / 极端日排行 / 热力图点周 |
| 建议依据 | 决策层 [依据⋯] 弹层（较30日/爬坡/容量公式；与 ⓘ/☰ 互斥） |
| 演示模式 | `D` 进入，←/→ 切幕，`Esc` 退出 |
| **深浅主题** | 顶栏 ☾/☀ 按钮切换，localStorage 持久化；两套色板均过 CVD 验证（浅 #0891B2/#C2620A，深 #0891B2/#D97706），切换时图表整体重渲换装 |
| 口径 / 导出 | ⓘ 弹层（与叠加选项互斥）；⤓ CSV（可见窗口全系列） |
| 悬浮读值 | 全图 crosshair 联动；时光机/热力图/小图 tooltip 挂 body 防裁切；偏差带 NOW 右侧虚线=预测追踪 |

## 演示剧本（六幕 ≈ 3.5 分钟）

纵深（14 年热力图）→ 节律（区域对比）→ 推演（参照系齐全的 fan chart）→ **验证（极涡重演，预测与真实对质）** → 自证（覆盖率审计）→ 落地（换省级数据即生产系统）。

## 数据替换契约（接 Supabase / 真模型）

原型数据为形态校准的仿真（真实 PJM 量级 + 2014 极涡/2012 热浪）。替换点集中在：

1. `loadAt(zone, ts)` → 查询/缓存 `energy_hourly` 小时值
2. `buildDaily()` → 按日聚合查询（或物化视图）
3. `forecastAt(zone, originTs)` → 真模型契约 JSON：`{ts, p10, p25, p50, p75, p90}[]`
4. `BT`（回测）与 `CAL`（残差标定）→ 真模型离线回测结果喂入；`RECORD` → 真实历史极值

其余渲染/交互层不动。真模型就绪前，页面内相似日基线 + 残差经验分位标定本身就是可讲的 Baseline（GEFCom2014 冠军范式的轻量实现）。

## 质量记录

- 色板：浅/深两版均通过 dataviz 六项检查（亮度带/彩度/CVD/对比度）
- 回测（仿真数据）：MAPE 2.2–2.8% · P90 覆盖 86–88% · P50 覆盖 46–48%，自检徽章点亮
- **基线对比（页面内实时复算）**：持久性（昨日同时）MAPE 3.9% → 本模型 2.34%（误差↓40%）；极涡重演段诚实显示"天气突变落后于基线——相似日盲区，正是接入气象预报的论据"（演示话术资产，勿删）
- 专业性收尾五项：P50（最可能）/P90（最坏情形）注解、MAPE 参照（优<3%）、偏差带 ±3% 关注线、口径补建议容量公式与气象输入假设、持久性基线对比
- **外部 AI 评审修复（2026-08-29）**：①偏差卡加权平均漏除权重和（数值放大 2.45×）已修；②弹层互斥；③胶片面积填充随区域变色；④偏差带 NOW 右侧改虚线（语义区分：已发生 vs 预测追踪）；⑤天气湿度/风速/降水在口径标注"演示占位"；⑥覆盖率阈值统一（P90±6/P50±8，条色与徽章一致）；⑦LIVE 徽章改"演示数据·锚定 2018-08"；⑧prepMW 包络口径说明；⑨forecastAt 加 (zone,origin) memo（注意：buildCal 后必须清缓存，否则 backtest 吃到未标定结果）
- 交互实测通过：区域切换 / 胶片拖拽 / 极端日跳转 / 热力图点击（隐藏容器 0×0 初始化 bug 已修：renderHeat 强制 resize）/ 演示模式六幕 / tooltip appendToBody 防裁切
- 已知限制：SHAP 已替换为诚实归因；时区统一 EST 展示；无移动端（演示场景为桌面/投影）

## 截图（按用途取用）

`.shots/v14_hero_merge_dark.jpeg`（v14 深色·主角化+合并终稿）· `v14_hero_merge_light.jpeg`（v14 浅色终稿全页）· `v13_dark.png` / `v12_review_fixed.png`（v13 前基线）· `v7_conclusion.png`（决策层特写）· `v9_heat_fixed.png`（季节热力图）· `v10_tooltip.png`（tooltip 置顶防裁切）

## 版本小史（讲演进时用）

1. 深色「决策中台」原型（时光机/扇形图/演示模式奠基）
2. 浅色化 + 问题式信息架构（数据必须回答用户问题）
3. 一栏三层框架（终结左右分栏联动、口径弹层化）
4. 决策层置顶 → 结论主导（建议句 26px 当主角，删不可解释的置信分）
5. 高度与密度调优（主图 37vh 自适应，首屏收齐决策+状态+主图）
6. 外部 AI 评审修复（偏差 2.45× 红线 + 6 小项 + 3 讨论点，含 memo 回归拦截）
7. 深浅双主题（CSS 令牌化 + 双 CVD 调色板 + 切换持久化）
8. **主体化 + 减密 + 时光机合并（v14，用户设计重审）**：①关键数字 46px 真主角（26px 结论句
   在四个 20px 数字面前不构成层级）；②参照系改按需呈现——依据行 6→3 项、四格/图例/频率句减密，
   被裁项入 title 或依据弹层（渐进披露："深度是发现的，不是陈列的"）；③时光机胶片 dock 进主图卡
   （屏+进度条一体的播放器隐喻，原 filmCard 删除），证据层上移；首屏 879px 一屏收齐
