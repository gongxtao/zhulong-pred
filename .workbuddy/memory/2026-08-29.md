# 2026-08-29 工作日志

## 全屏铺满改造（load-forecast-prototype.html）

用户要求"页面内容铺满整个屏幕"。

### 改动
- `.wrap` 去掉 1320px max-width 限制 → `width:100%;max-width:none;padding:20px 24px 48px`
- `#main` 主图高度由固定 470px 改为 `clamp(430px,56vh,780px)`，随视口自适应
- `#backtest` 回测图由 270px → `clamp(270px,34vh,420px)`
- `.sm .chart` 小图由 128px → `clamp(128px,16vh,200px)`
- 主图 grid 改用百分比：`{top:46,height:'58%'}` + `{top:'74%',height:'13%'}` —— 关键修复，否则自适应高度下温度副带会脱离 dataZoom
- 设计决策说明 1600px 以上改 3 列（之前两列在全屏下拉得过宽）

### 验证
- 1920×1080 视口下截图三段（顶部/中段/底部）
- 顶部：KPI 4 列平铺，主图横跨全宽，左 24px / 右 24px 内边距正确
- 中段：dataZoom slider 正常显示，三区域对比 3 列平均
- 底部：回测 1fr+292px 比例正常，设计说明 3 列排版

### 工具链踩坑
- agent-browser 需要装在 `/Users/mac/.workbuddy/binaries/node/workspace` 而非全局（受管运行时规则）
- Chromium 下载 `agent-browser install` 在沙箱里被 SIGKILL（exit 137），但 native 二进制本身可用，open 命令能自动找到已存在的 browser
- 视口设置：`set viewport 1920 1080`（不是 set-viewport / viewport）
- 全页截图：`screenshot --full`（短横线），不是 `--full-page`
